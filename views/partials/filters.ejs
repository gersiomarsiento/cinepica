<div class="filters-container" id="filters-container">
  <nav class="filters-navbar" id="filters-navbar">
    <div class="filter-navlinks">
      <p>Filtrar por:</p>
      <div id="filter-selector">
        <!-- ONE FILTER PER EACH OF THE FILTER CATEGORIES -->
        <% for (let category of filterCategories){ %>
          <ul class="filter-by <%= (category=="director")?"active":""%>">
            <h3 class="filter-by-title">
              <span class="filter-by-button filter-by-button-prev" id="filter-by-button-prev"><i class="fa-solid fa-arrow-left"></i></span>
              <%= filterCategoriesSpa[filterCategories.indexOf(category)] %> 
              <span class="filter-by-button filter-by-button-next" id="filter-by-button-next"><i class="fa-solid fa-arrow-right"></i></span>
            </h3>
            <button class="scroll-up-filter"><i class="fa-solid fa-angles-up"></i></button>
            <!-- LOOP OVER FILTERED CATEGORY ELEMENTS -->
            <div class="filter-elements-container">
              <% for (let value of new Set(films.flatMap(x => x[category]).sort())) { %>
                <%if (value == "" || value == null) {%>
                  <% continue; %>
                <%} else {%>
                  <a class="filter-element filter-toggler hide" data-category="<%= category %>" data-value="<%= value %>">
                    <%= category=="genres"? value.charAt(0).toUpperCase(0) + value.slice(1) : value %>
                  </a>
              <% }} %>
            </div>
            <button class="scroll-down-filter"><i class="fa-solid fa-angles-down"></i></button>
          </ul>
        <% } %>
      </div>
    </div>
  </nav>
</div>